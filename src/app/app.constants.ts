// Static brand: the product name is the same in both UI languages (mirrored in index.html metas and in the
// manifest of vite.config.ts).
export const APP_BRAND = { name: 'Pronunciador', tagline: 'EN → ES' } as const;
